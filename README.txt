Twitter Sentiment Analysis:

This script provides us the sentiment of the tweets on a particular topic or object mentioned. 

"tweepy" is used as a wrapper to use twitter api to extract tweets.

"textblob"( library for processing textual data) is used to provide the sentiment polarity of the tweet. It can setermine whether the tweet was positive, negative or neutral towards the "keyword" it was used for.
