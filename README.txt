Twitter Sentiment Analysis:

"tweepy" is used as a wrapper to use twitter api to extract tweets.

"textblob"( library for processing textual data) is used to provide the sentiment polarity of the tweet. It can determine whether the tweet was positive, negative or neutral towards the "keyword" it was used for.
